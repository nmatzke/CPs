
bin/readme.txt

bin = binaries = a program/executable file, produce by compiling source code

Sometimes the same binary will run on one computer, but not on another computer; in that case the source has to be recompiled for the other computer.

